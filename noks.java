import java.util.Scanner;

public class addDrop(
Scanner in = new Scanner(System.in);System.out.print("Введите название игрушки: ");
String name = in.nextLine();
Scanner in1 = new Scanner(System.in);System.out.print("Введите шанс выпадения игрушки: ");
int chance = in1.nextInt();)

public class priz(
// розыгрыш игрушек
Scanner in = new Scanner(System.in);System.out.print("Угадайте число от 1 до 2 что бы выйграть игрушку: ");
int num = in.nextInt();
int x = 0;while(x!=1){
        if(num == 1 || num == 2){
            x = 1;
        }
        else{ 
            System.out.println("введено не коректное занчение")
        }
        })
        /////////////////////////
        (
        Scanner in = new Scanner(
                System.in);System.out.print("Ура вы угадали, выберите из списка номер понравившейся игрушки: ");
int num = in.nextInt();x=0;while(x!=1){
    
    try{
        x = 1;
    } catch (){

    }
})
        // нажмите что бы выбрать игрущку (записываем в список выбранныз они ожидают
        // выдачи)
        ///////////////////////////
        (

    public static void main(String[] args) {
        
        try(FileWriter writer = new FileWriter("priz.txt", false))
        {
           // запись всей строки
            String text = "Hello Gold!";
            writer.write(text);
            writer.flush();
        }
        catch(IOException ex){
            System.out.println(ex.getMessage());
        } 
    })
// метод получения ее(удаляем из массива и пишем в текст какие игрушки выбили)